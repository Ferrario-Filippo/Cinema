@model Ticket
@{
	ViewData["Title"] = "Elenco Biglietti";
}

<h1>@nameof(Ticket)</h1>

<div class="container p-3">
	<div class="row pt-4">
		<div class="col-6">
			<h2 class="text-primary">Elenco Biglietti</h2>
		</div>
		<div class="col-6 text-end">
			<a asp-controller="Ticket" asp-action="Upsert" class="btn btn-primary">
				<i class="bi bi-plus-circle"></i> &nbsp; Crea nuovo Biglietto
			</a>
		</div>
	</div>
	<br /><br />

	<table id="tblData" class="table table-bordered table-striped" style="width:100%">
		<thead>
			<tr>
				<th>Numero</th>
				<th>Fila</th>
				<th>Proiezione</th>
				<th>Costo</th>
				<th>Utente</th>
				<th></th>
			</tr>
		</thead>
	</table>
</div>
@section Scripts{
	<script src="~/js/tickets.js"></script>
}