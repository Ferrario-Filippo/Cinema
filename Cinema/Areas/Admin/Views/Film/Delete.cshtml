@model Film
@{
	ViewData["Title"] = $"Elimina {nameof(Film)}";
}

<form method="post" asp-action="Delete">
	<input asp-for="FilmId" hidden />
	<div class="border p-3 mt-4">
		<div class="row pb-2">
			<h2 class="text-primary">Elimina @nameof(Film)</h2>
			<hr />
		</div>
		<div class="mb-3">
			<label asp-for="Title"></label>
			<input asp-for="Title" disabled class="form-control" />
		</div>
		<button type="submit" class="btn btn-danger" style="width:150px">Elimina</button>
		<a asp-controller="Film" asp-action="Index" class="btn btn-secondary" style="width:150px">
			Indietro
		</a>
	</div>
</form>

@section Scripts{
	@{
		<partial name="_ValidationScriptsPartial" />
	}
}
